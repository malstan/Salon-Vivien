/**
 * configuration for database connection
 * @type {{PASSWORD: string, HOST: string, USER: string, DB: string}}
 */

module.exports = {
    HOST: "localhost",
    USER: "root",
    PASSWORD: "",
    DB: "salon_vivien"
};